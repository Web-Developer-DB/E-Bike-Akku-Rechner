/**
 * Test environment setup.
 *
 * This import adds convenient DOM assertions such as toBeInTheDocument() to
 * Vitest's expect API. It runs before every test file through vite.config.ts.
 */
import '@testing-library/jest-dom/vitest';
